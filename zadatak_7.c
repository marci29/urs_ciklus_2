/* 7 . Prikazan je dio izvornog koda koji koristi TCA0 i PWM (single slope način rada) za generiranje PWM signala s RADNIM OMJEROM 75% na pinu PD2 (korištenjem CMP2 compare kanala). PERIOD = 1ms… Nadopuni kod koristeći hexa vrijednosti… (pogledati primjer u skripti)… (6bodova)

// postavi mod single slope PWM
// omogući CMP2
// izračunaj period iz F_CPU = 3333333UL
// izračunaj vrijednost compare reg
// poveži cmp2 s PORTD
// pokreni TCA0
While(1); */
